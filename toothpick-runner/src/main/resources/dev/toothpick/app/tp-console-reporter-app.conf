app {
  api-client {
    include required(classpath("dev/toothpick/app/shared/api-client.conf"))
  }

  reporter {
    include required(classpath("dev/toothpick/app/shared/reporter.conf"))
  }
}

log.level = info
